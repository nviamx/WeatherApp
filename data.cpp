/*
#include <iostream> 

using namespace std;
// Constructor
WeatherData::WeatherData(string cityName, string latitude, string longitude,float temp, float maxTemp, float minTemp, float hum, float windSpd, float pres, const std::string& desc, bool fav)
    :cityName(cityName), latitude(latitude), longitude(longitude),temperature(temp), maxTemperature(maxTemp), minTemperature(minTemp), humidity(hum), windSpeed(windSpd), pressure(pres), description(desc), favorites(fav) {


}

// Function to print all the data
void WeatherData::printData()  {
    cout << "Current Temperature: " << temperature << "°C" << endl;
    cout << "Max Temperature Today: " << maxTemperature << "°C" << endl;
    cout << "Min Temperature Today: " << minTemperature << "°C" << endl;
    cout << "Humidity: " << humidity << "%" << endl;
    cout << "Wind Speed: " << windSpeed << " km/h" << endl;
    cout << "Pressure: " << pressure << " hPa" << endl;
    cout << "Description: " << description << endl;
}


*/